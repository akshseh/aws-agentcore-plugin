{
  "tools": [
    {
      "name": "get_order_status",
      "description": "Returns order status and tracking information",
      "inputSchema": {
        "type": "object",
        "properties": {
          "order_id": { "type": "string", "description": "Customer order ID" }
        },
        "required": ["order_id"]
      }
    },
    {
      "name": "search_kb",
      "description": "Searches the product FAQ knowledge base",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": { "type": "string", "description": "Search query" }
        },
        "required": ["query"]
      }
    }
  ]
}
